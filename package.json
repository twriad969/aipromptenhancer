{
  "name": "your-app-name",
  "version": "1.0.0",
  "description": "A Node.js application using Google Generative AI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@google/generative-ai": "^1.0.0", // Adjust version as needed
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
